<section class="page-width notfound">
  <p class="eyebrow eyebrow--accent">ERROR 404</p>
  <h1 class="notfound__title">Esta pared está vacía.</h1>
  <p class="page-head__subtitle">La página que buscas no existe o se ha movido.</p>
  <div class="story__cta">
    <a class="button" href="{{ routes.all_products_collection_url }}">Ver obras</a>
    <a class="button button--outline" href="{{ routes.root_url }}">Volver al inicio</a>
  </div>
</section>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
