<section class="page-width notfound">
  <p class="eyebrow eyebrow--accent">{{ 'notfound.eyebrow' | t }}</p>
  <h1 class="notfound__title">{{ 'notfound.title' | t }}</h1>
  <p class="page-head__subtitle">{{ 'notfound.text' | t }}</p>
  <div class="story__cta">
    <a class="button" href="{{ routes.all_products_collection_url }}">{{ 'general.see_works' | t }}</a>
    <a class="button button--outline" href="{{ routes.root_url }}">{{ 'notfound.home' | t }}</a>
  </div>
</section>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
